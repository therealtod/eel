package testcommon

import eelst.ilike.game.entity.player.PlayerMetadata

object CommonData {
    val aliceMetadata = PlayerMetadata("Alice", 0)
    val bobMetadata = PlayerMetadata("Bob", 1)
    val cathyMetadata = PlayerMetadata("Cathy", 2)
    val donaldMetadata = PlayerMetadata("Donald", 3)
    val emilyMetadata = PlayerMetadata("Emily", 4)
}
